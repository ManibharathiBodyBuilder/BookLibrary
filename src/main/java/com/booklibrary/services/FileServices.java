package com.booklibrary.services;

/*import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.booklibrary.entity.FileModel;
import com.booklibrary.repository.FileRepository;

@Service
public class FileServices {
	@Autowired
	private FileRepository fileRepository;

	
	public void UploadDoc(FileModel fileModel) {
		fileRepository.save(fileModel);
	}

	
	public List<FileModel> GetAllFile() {
		return fileRepository.findAll();
	}

	
	public FileModel GetById(Long fileId) {
		return fileRepository.findById(fileId).get();
	}
}
*/